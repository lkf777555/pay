<view class="container">
  <!-- 轮播图 -->
  <i-swiper data="{{bannerData}}"></i-swiper>

  <!-- 扫码按钮 -->
  <i-code-button count="{{count}}" status="{{status}}"  bind:getResult="getShopCode"></i-code-button>

</view>