<view class="swiper">
  <swiper>
    <block wx:for="{{bannerArr}}" wx:key="{{item.id}}">
      <swiper-item>
        <image src="{{item.url}}"></image>
      </swiper-item>
    </block>

   
  </swiper>
</view>

<!-- 1. 创建小程序项目
2. 创建tabbar页面
3. 配置tabbar
4. 对wx.request进行二次封装(使用class类以及promise)

  4.1 在utils目录下创建request.js

  4.2 创建HTTP类

  4.3 创建request方法和_request方法

  4.4 在request方法里面调用_request方法

  4.5 在_request方法里面调用wx.request方法

  4.6 为了能够使用.then的方式接受请求的数据,我们在request方法里面使用promise进行处理

  ....

5. 对api接口进行封装

  5.1 在目录创建api文件夹

  5.2 在api文件夹内创建对应的js接口文件

  5.3 导入封装HTTP类

  5.4 继承HTTP类的方法封装API接口方法

  5.5 导出封装的api接口

6. 在页面调用封装的接口,将数据渲染到页面(视图)

  6.1 导入封装的api接口文件

  6.2 进行实例化

  6.3 直接调用封装的api接口方法 -->